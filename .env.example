# Caprae LeadGenius AI - Environment Configuration
PORT=3000
NODE_ENV=development

# Optional: PostgreSQL Database Connection (AWS Aurora / Supabase / Neon)
# If left commented or empty, the app defaults to the embedded Bounded LRU Cache & seed dataset.
# DATABASE_URL="postgresql://postgres:password@localhost:5432/caprae_leadgenius?schema=public"

# Optional: Redis Distributed Cache Connection (AWS ElastiCache / Upstash)
# REDIS_URL="redis://localhost:6379"

# Application Metadata
NEXT_PUBLIC_APP_VERSION="3.1.0-caprae-production"
